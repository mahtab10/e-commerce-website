
#### 7) Create a README file to answer the following questions-

> **⚠️ Warning:** Do not use any AI tools to answer these questions. You must write the answers in **Bangla**.

#### 1) What is the difference between `null` and `undefined`?
Null: নাল মানে কোন ভেরিয়েবল কে ইচ্ছে করে খালি রাখা। let x = null.
Undefine: আনডিফাইন মানে কোন মান দেওয়া হয় না। let y.

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?
Use of map(): ম্যাপ ফাংশন নতুন এরে বানায় আগের এরেকে ঠিক রেখে।
for each() শুধু লুপ চালায়, নতুন কিছু বানায় না।

#### 3) What is the difference between `==` and `===`?

== শুধু মান চেক করে, === ডেটা টাইপ ও মান চেকে করে

#### 4) What is the significance of `async`/`await` in fetching API data?
async একটি এসিনক্রোনাস ফাংশন, এটি একটি প্রমিস রিটার্ন করে। 
await ফাংশন প্রমিসের জন্য অপেক্ষা করে।

#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).

Global scope : গ্লোবাল্লি ডিক্লেয়ার করা হয় এবং পুরু ফাইলে ব্যবহার করা যায় 
Fynction scope: একটি ফাংশনে ডিক্লেয়ার করা হয় এবং ওই নির্দিষ্ট ফাংশনেই ব্যবহার করা যায় 
Block scope: কোন ফাংশনের ব্লকের ভিতর যদি declere করা হয় তাহলে শুধুমাত্র সেই ব্লকের ভিতরে ব্যবহার করা যায়।
